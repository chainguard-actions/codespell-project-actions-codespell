name: Test basic usage - problem matcher registration
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run codespell problem matcher action
        id: action
        uses: .

      - name: Assert action completed successfully
        run: |
          echo "PASS: codespell problem matcher action ran successfully"
          echo "The action emits ::add-matcher:: to register the codespell problem matcher"
